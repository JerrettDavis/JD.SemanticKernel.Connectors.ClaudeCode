namespace PullRequestReviewer.Abstractions;

/// <summary>
/// A review comment generated by the agent for a specific file and location.
/// </summary>
public sealed record ReviewComment
{
    /// <summary>The file path this comment pertains to.</summary>
    public required string FilePath { get; init; }

    /// <summary>The line number in the diff (if applicable).</summary>
    public int? LineNumber { get; init; }

    /// <summary>Severity of the finding.</summary>
    public required ReviewSeverity Severity { get; init; }

    /// <summary>Category of the finding (e.g., "Security", "Performance", "Style").</summary>
    public required string Category { get; init; }

    /// <summary>The review comment body.</summary>
    public required string Body { get; init; }

    /// <summary>Optional suggested code fix.</summary>
    public string? SuggestedFix { get; init; }
}

/// <summary>Severity levels for review findings.</summary>
public enum ReviewSeverity
{
    Info,
    Warning,
    Error,
    Critical
}
